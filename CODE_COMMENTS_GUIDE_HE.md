# מדריך ההערות בקוד

בגרסה הזאת נוספו הערות בעברית לכל קובצי הקוד המרכזיים ב-Frontend, Backend, Docker, CI, CSS, HTML ו-SQL.

ההערות נמצאות לפני כל מחלקה, קומפוננטה, פונקציה ובלוק לוגי מרכזי. המטרה היא להסביר את הזרימה בלי להעמיס הערה על כל שורת קוד פשוטה.

## קבצים ללא הערות בתוך הקובץ

`package.json` ו-`package-lock.json` הם קובצי JSON. תקן JSON אינו מאפשר Comments, ולכן לא נוספו להם הערות כדי לא לשבור את ההתקנה. ההסבר שלהם נמצא ב-README ובמסמכי התיעוד.

## סוגי ההערות

- JavaScript ו-JSX: `//` ו-`/** ... */`
- CSS: `/* ... */`
- YAML ו-Dockerfile: `#`
- HTML: `<!-- ... -->`
- SQL: `--`
